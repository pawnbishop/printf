#include "main.h"
#include <stdio.h>

/**
 * _string - returning lenght of str
 * @str: a pointer
 *
 * Return: 1 
 */

int _string(char *str)
{
	int prt;
	
	for (prt = 0; str[int] != '\0'; prt++);
	return (prt);
}
